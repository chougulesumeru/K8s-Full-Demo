# 03 service.yml

apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: demo-ns
spec:
  selector:
    app: demo-ns
  ports:
    - port: 80
      targetPort: 80
      nodeport: 30080
  type: NodePort
